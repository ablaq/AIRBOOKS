<h1>Books#my_books</h1>
<ul>
  <% @books.each do |book| %>
    <% if book.user == current_user %>
      <%= link_to book_path(book.id) do %>
        <li> <%= book.title %> - <%= book.author%>
      <% end  %>
    <% end  %>
  <% end  %>
</ul>
